#include "include/thumbnailViewer.h"

int main(int argc, char *argv[]) {
    /* Set the path folder in the thumbnail constructor
       in the src directory, specify absolute path from
      the root directory */
    ThumbnailViewer obj(argc, argv);
}